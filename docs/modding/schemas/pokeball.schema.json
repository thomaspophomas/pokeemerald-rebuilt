{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pokeemerald-rebuilt.local/schemas/pokeball.schema.json",
  "title": "Mod pokeball",
  "oneOf": [
    {
      "$ref": "#/$defs/itemList"
    },
    {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pokeballs": {
          "$ref": "#/$defs/itemList"
        }
      },
      "required": [
        "pokeballs"
      ]
    }
  ],
  "$defs": {
    "itemList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/item"
      }
    },
    "item": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "mode": {
          "type": "string",
          "enum": [
            "observe",
            "modify",
            "replace",
            "claim"
          ],
          "default": "observe"
        },
        "itemId": {
          "$ref": "#/$defs/token"
        },
        "item_id": {
          "$ref": "#/$defs/token"
        },
        "ballId": {
          "$ref": "#/$defs/token"
        },
        "ball_id": {
          "$ref": "#/$defs/token"
        },
        "catchModifier": {
          "$ref": "#/$defs/token"
        },
        "catch_modifier": {
          "$ref": "#/$defs/token"
        },
        "modifierHook": {
          "$ref": "#/$defs/cIdentifier"
        },
        "modifier_hook": {
          "$ref": "#/$defs/cIdentifier"
        },
        "commitHook": {
          "$ref": "#/$defs/cIdentifier"
        },
        "commit_hook": {
          "$ref": "#/$defs/cIdentifier"
        },
        "battleScript": {
          "$ref": "#/$defs/cIdentifier"
        },
        "battle_script": {
          "$ref": "#/$defs/cIdentifier"
        },
        "flags": {}
      }
    },
    "token": {
      "oneOf": [
        {
          "type": "integer"
        },
        {
          "type": "string",
          "minLength": 1
        }
      ]
    },
    "s16": {
      "type": "integer",
      "minimum": -32768,
      "maximum": 32767
    },
    "u16": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535
    },
    "cIdentifier": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
    },
    "shortString": {
      "type": "string",
      "minLength": 1,
      "maxLength": 31
    }
  }
}
